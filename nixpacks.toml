[phases.setup]
nixPkgs = ["php82", "php82Extensions.pdo", "php82Extensions.pdo_mysql", "php82Extensions.mysqli"]

[phases.install]
cmds = ["echo 'Nixpacks PHP Extensions Installed'"]

[variables]
PHP_EXTENSIONS = "pdo_mysql,mysqli"
